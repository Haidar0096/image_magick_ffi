name: image_magick_ffi_example
description: Demonstrates how to use the image_magick_ffi plugin.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


environment:
  sdk: '>=2.18.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  image_magick_ffi:
    path: ../
  file_picker: ^5.2.1

  path_provider: ^2.0.11

  permission_handler: ^10.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true